# LoPo 開発ToDo

> 完了済みタスクは [TODO_COMPLETED.md](./TODO_COMPLETED.md) に移動済み

---

## バグ
- [ ] LP: THREE.Clock非推奨警告 — THREE.Timerに移行が必要（動作には影響なし）
- [ ] FFLogsインポート: 英語主言語のログで言語取得できない問題
- [ ] FFLogsインポート: 無敵で0にしたダメージ、リビングデッド中のダメージが正しく反映されない
- [ ] オートプラン: 無敵はなるべく同じ技に対して使うようにしたい（タイミングが違っても）
- [ ] Googleログイン画面に「lopo-7793e.firebaseapp.com」が表示される — Blazeプラン移行済み、カスタムドメイン設定で修正可能
- [ ] **アプリ全体の動作が重い** — ログアウト並列化は完了。残り: 起動時Firestore読み込みの非ブロッキング化、React.memo追加（視覚変更後に対応）

## Stripe/Ko-fi
- [ ] **Stripe審査結果待ち** — 追加情報提出済み（2026-03-27）。結果を確認する
- [ ] **Stripe審査通過後：フッターの法的リンクをまとめる** — プライバシー・利用規約・特商法の3リンクをドロップダウン等にまとめてフッターを短縮する

## 次セッション予定（第35セッション）
- [ ] **軽減配置時のフィードバックアニメーション** — 配置時に効果範囲を一瞬アニメ表示、D&D中は効果終了イベントをツールチップ表示
- [ ] **AA追加モードのフロー刷新** — フローティングバー（選択共有/削除UIパターン流用）で統合フロー
- [ ] **モーダル・画面のデザイン改善 + アクセントカラー**（管理基盤完了後）
- [ ] **パフォーマンス最適化**（全ての視覚変更後）
- [ ] **public/icons/ 削除** — 全動作確認完了後に実施（バンドル2.1MB削減）

## UI改善（要対応・要相談）
- [ ] **プラン未選択時の空パネルデザイン刷新** — Liquid Glass実装済みだがデザイン見直しは継続検討

## バグ（明確に壊れている）
（完了済みバグは TODO_COMPLETED.md に移動済み）

## 確認・調査が必要
- [ ] **パルス・設定の保存確認** — 保存されているか？ログインなら端末またぎも同期される？（スマホではパルスは見えないのでOK）
- [ ] **プラン削除の端末間同期遅延** — PCでプランを削除→スマホ(同アカウント)でリロードしてもなかなか消えない。同期タイミングの調査が必要
- [ ] **ユーザーに優しくない表現の精査** — 「OGP画像」など技術用語がUIに残っていないか全体チェック
- [x] ~~**Discord通知刷新**~~ — 完了。TODO_COMPLETED.md に移動済み
- [ ] **管理画面への導線** — 管理者のGoogleアカウントでログイン中なら、右上のユーザーアイコンから /admin に遷移できるようにする。URLを手打ちしなくて済む（2026-03-28 第33セッション）
- [ ] **LP・人気ページにログイン状態の表示がない** — ユーザーが自分がログインしているか分からない。ヘッダーにアイコンやログインボタンを表示すべきか検討（2026-03-28 第33セッション）

## デザイン改善（モーダル・画面の見直し）
以下の画面はライトモードでデザイン的な見直しが必要:
- [ ] ステータス表示
- [ ] フェーズ追加モーダル
- [ ] 共有プレビュー作成画面
- [ ] 削除確認画面
- [ ] オートプラン実行画面（きれいかも？要確認）
- [ ] FFLogsインポート画面（透明感はきれい、きれいかも？要確認）
- [ ] ログイン画面（同上）

## ツールチップ
- [ ] **ダーク・ライト共通で色を反転すべきか** — 要検討

## UX改善
- [ ] **AA追加モードのフロー刷新**（第34セッション アイデア）
  - 現行: 歯車で設定→追加モード→クリックで配置
  - 提案: ボタン押下→設定モーダルが開く（「オートアタックとして追加するダメージを入力してください」）→ダメージ入力→「追加開始」で追加モードに入る→マウスにツールチップが追従（「AA: 50000 をクリックで追加」のような表示）→タイムラインクリックで配置
  - 目的: 現在のフローは歯車の位置が分かりにくく、ダメージ設定が追加モードと分離している。一連の流れとして統合することでUXを改善する
- [ ] **サイドバー「開発・支援」** — サイドバーを畳んだらKo-fiリンクを消す or Ko-fiロゴだけにする

## アクセントカラーの方針（要相談）
- [ ] 警告系 → 黄色？
- [ ] 削除・危険系 → 赤？
- [ ] OK・先に進む系 → 青にする？それとも白黒のまま？

## 公開前に必要（機能）
- [x] **管理者用リファレンスファイル作成** — `ADMIN_REFERENCE.md`（.gitignore対象）。今後増えたら追記する
- [ ] **アプリ動作パフォーマンスの最適化** — 公開前に改善必須
- [ ] **サイドメニュー・ヘッダーの開閉パフォーマンス最適化** — React.memoで対応可能
- [x] **管理基盤・マスターデータFirestore移行（Phase 0-4 全完了）** — 第33セッションでPhase 4完了。Firebase Storage有効化・アイコンシード・デプロイ・動作確認済み。残り: public/icons削除のみ。設計書 → `docs/管理基盤設計書.md`
- [x] **プラン複製機能** — Phase 0で実装済み
- [ ] **軽減のないヒールスキル（テトラ、ディグニティ等のoGCDヒール）をタイムラインに配置可能にする**

## スマホ対応 残タスク
- [ ] スマホ対応（追加改善: モーダルのレスポンシブ最適化など）
- [ ] タブレット表示の最適化
- [ ] スクショからダメージ・軽減を判別する機能（将来のスマホ向け）

## チュートリアル（残タスク）
- [ ] 追加ステップ候補: オートプラン体験・プラン名編集・共有体験
- [ ] チュートリアル中の戻るボタンの通し動作確認（特にパーティ系ステップの巻き戻し）

## 未着手（機能・将来）
- [ ] 古いプランの自動アーカイブ — 30件超過時、現行零式・絶以外の30日以上未編集プランをFirestoreに圧縮退避
- [ ] SA法オートプランナー改善
- [ ] ゲーム内風リキャスト表示 — 円形クールダウンタイマー（優先度低・必要性要検討）
- [ ] **詠唱バー注釈機能** — ボスの詠唱バー上にメモ。実装方法の検討が必要
- [ ] AI APIでオートプラン — 無料で数回試せる、課金モデル

## アイデア・やりたいこと

### Discord改善（2026-03-28 第25セッション、第34セッション追記）
- **アップデート通知** — 再検討: そもそもWebhookでのアプデ通知自体が不要かもしれない。コミットログをそのまま流しても一般ユーザーには分かりにくい。手動で書いた方が意味のある告知になる。廃止を検討中（第34セッション）
- **ワンポチ通知オフ** — Discordのサーバーオンボーディング機能やルールスクリーニングで対応可能（BOT不要）
- ~~管理者通知BOT~~ → BOTは常時稼働が必要でコスト・管理が重いため見送り

### トップページ
- こだわり抜いたヒーロー配置のトップページにしたい
- AIっぽいデザインは絶対NG（CLAUDE.mdのルール通り）

### ログイン体験
- メール+パスワードログインも将来的に検討？

### ログイン/保存/共有の仕様（確定）
- 未ログイン: 軽減表の作成・編集・共有OK。保存はブラウザのみ（消える可能性あり）
- ログイン済: 上記 + 軽減表の永続保存 + チームロゴ設定 + OGPにロゴ表示
- **FFLogsインポートはログイン限定**（2026-03-25確定）
- チームロゴの画面内常時表示はしない。OGP画像のみ（2026-03-25確定）

### マネタイズ方針（確定 2026-03-25、Ko-fi導入 2026-03-26）
- アプリ内課金・広告なし（SE利用規約準拠）
- YouTubeチャンネルへの誘導
- **Ko-fi (ko-fi.com/lopoly)** — Stripe連携済み、$3〜の支援受付。週次自動入金
- 悪意あるAPI消費ユーザーはFirebase Auth無効化でBAN可能

### サイドバー・操作フロー
- 共有ページの閲覧専用である明示を強化
- 色はまず全体を白黒ベースにしてからアクセントカラーを入れる（2026-03-24方針）
- プラン最大件数は5件。古いプランの圧縮保存期間は30日かもっと短くてもいい

### 軽減配置時のフィードバックアニメーション（第34セッション アイデア）
- **配置時**: 軽減をクリックで配置した瞬間、その軽減の効果時間がどこまで（何秒の何イベントまで）カバーするかを、かわいいアニメーションで一瞬見せる
- **ドラッグ中**: 軽減をD&Dで移動するとき、効果時間内の最後のイベント（または効果終了地点のイベント）を既存のツールチップスタイルでリアルタイム表示する
- 目的: 軽減の「効果範囲」を直感的に把握できるようにする。今は配置後にタイムラインを目視確認するしかない

### ~~スプレッドシートへのデータエクスポート~~ → CSVエクスポート（第34セッション 実装済み）
- サイドバーのプラン⋮メニューから「CSVダウンロード」で軽減表データをエクスポートできる
- BOM付きUTF-8、イベント×適用中軽減の一覧形式
- Sheets API連携は不要と判断（CSV十分、OAuth追加は逆効果のリスク）

### UI全般
- 細かいUIの温度感・アニメーション不足を改善

### フッター統一ルール（確定 2026-03-27、特商法追加済み）
- ランディングページ: Discord + Ko-fi + 規約 + 特商法 + 権利表記
- 人気ページ: Discord + Ko-fi + パルス設定 + 規約 + 特商法 + 権利表記
- 軽減表アプリ: Discord + パルス設定 + 規約 + 特商法 + 権利表記（Ko-fiはサイドバーのまま）
- 今後の全ページ: フッターに権利表記 + 規約 + 特商法 + Discord + Ko-fi を必ず入れる

## 確定した設計方針（2026-03-28 第27セッション追記）

### 管理基盤・Firestore���行の設計方針（確定）
- 全ゲームデータ（コンテンツ・テンプレート・スキル・ステータス・アイコン・DC/サーバー）をFirestoreに段階的移行
- 操作中のFirestoreアク���スゼロ（��動時1回フェッチ→localStorage+メモリキャッシュ→同期的に使用）
- バージョン番号でキャッシュ無効化（変更時のみ再取得）
- マスターデータは種類ごとに1ドキュメント（読み取り回数の最適化）
- 管理画面は `/admin` ルート（同一アプリ内、Custom Claims認証）
- テンプレート自動生成: FFLogsインポートから発見フェーズ（14日）→ロック → 人気プラン昇格（管理者承認制）
- アイコンはFirebase Storage + Vercelエッジキャッシュで配信
- Firebase App Check + APIレート制限 + 監査ログ + BAN機���
- ローカル開発は本番Firestoreに読み取り専用接続（静的ファイルはフォールバック用）
- 詳細設計書: `docs/superpowers/specs/2026-03-28-admin-master-data-platform-design.md`

### プラン複製機能の設計方針（確定）
- サイドバーのプラン名横にコピーアイコン（既存の鉛筆マークの隣）
- ツールチップ: JA「すぐ下にコピーを作成」/ EN「Create a copy just below」（既存のツールチップスタイル）
- ワンクリックで直下に「元の名前 (2)」として即座に複製（モーダルなし）
- プラン件数制限（1コンテンツ5件/合計50件）は通常の新規作成と同じチェック

### GoogleログインPWA対応の設計方針（確定）
- PWA（ホーム画面から起動）時のみ `signInWithRedirect` に切り替え
- PC版ブラウザは `signInWithPopup` のまま変更なし
- `window.matchMedia('(display-mode: standalone)')` で判定

## 確定した設計方針（2026-03-28 第26セッション追記）

### フリープラン（ダンジョン・レイド・その他）の設計方針
- 零式・絶はcontents.jsonのデータから選択、それ以外はユーザーが名前を自由入力
- 入力した名前がcontentIdとtitleの両方になる（1回の入力で済む）
- SavedPlanにcategoryフィールドを追加（サイドバーのグルーピングに使用）
- サイドバーではカテゴリ別に表示（零式/絶のようなシリーズ→階層の3段階ツリーではなく、カテゴリ→プラン名のフラット表示）
- Firestoreにもcategoryを保存・復元する

### NewPlanModalの設計方針
- レベル・カテゴリは常に未選択スタート（サイドバーからの直接作成は従来通り）
- コンテンツ選択はドロップダウンではなく1列フラットリスト（すぐ選べる）
- 未入力項目があれば案内メッセージ表示 + 作成ボタン無効
- Enterキーで作成実行可能

## 確定した設計方針（2026-03-28 第25セッション追記）

### サイドバー・ヘッダー線統一の方針（確定）
- glass-tier3クラス定義は触らない（23箇所で使用）
- 個別要素にglass-border-*-0で辺ごとにborderを除去する方式
- backdrop-filterのblur境界は要素を1pxオーバーラップさせて隠す
- ヘッダーハンドル上辺の微細なblur重なりはCSS構造的限界として許容
- 対象はサイドバー枠線・ヘッダー枠線・両者の接合部のみ。タイムラインは触らない

## 確定した設計方針（2026-03-27 第24セッション追記）

### ガラス表現の視認性向上方針
- タイムライン枠: ボーダー光沢 + 影（ダーク:グレー線+にじみ、ライト:黒パキッと線）。blurは不要（中のテーブルが読めなくなる）
- ヘッダー区切り線: 今より明るくするだけ。影やglowは不要（小さすぎて効果がない）
- サイドバー選択中: 控えめなボーダー追加。枠として認識できる程度
- モーダル: 既にglass-tier3でそこそこ。ボーダーだけ合わせてもいい
- **全部に同じスタイルではなく、場所に合った強さにする**（ごちゃごちゃ防止）
- テーブル横罫線: ユーザーがトグルで選択可能（デフォルト非表示）
- 光の周回アニメーションは軽減表ページではやらない（作業中ずっと見る画面なので集中の邪魔になる）

### サイドバーのハイライト階層ルール（確定）
- 開いているプラン: 左インジケーター（border-l-2）で「今ここ」を明示
- アクティブなプランを含む大カテゴリ: 全周ボーダーで強調
- ただ展開しているだけの大カテゴリ: グレーハイライトのみ
- ヘッダー区切り線: ダーク=白25%、ライト=純黒

### パフォーマンス最適化の順序（確定）
- **視覚的な変更を全部終えてから最後にやる**
- 理由: UIの見た目を変えるとコンポーネント構造やpropsが変わる。先にメモ化すると見た目変更のたびにメモ化を壊したり調整し直す手間が増える
- React.memo / useMemo で対応予定

## 確定した設計方針（2026-03-24）

### サイドバーの操作フロー
- コンテンツクリック = 即編集開始。「テンプレート」という概念はユーザーに一切見せない
- テンプレートがあるコンテンツ → 裏で読み込み → 自動でプランとして保存 → 即編集
- テンプレートがないコンテンツ → 空プランを自動作成 → 即編集
- 同コンテンツに既存プランがある場合 → 即オープン（重複作成しない）
- 同コンテンツ1件以上 → サブアイテム展開。末尾に「+ 追加」行
- プラン名はサブアイテムのペンシルボタンでインライン編集（ヘッダーではダブルクリック）
- コンテンツ未紐付きプラン（自由作成）はレベル別「カスタム」セクションに表示
- 「新規作成」ボタンはレベルタブの上に控えめ配置

### 保存の仕組み（2層構造・確定方針 2026-03-25改定）
- **1層：localStorage（リアクティブ・無音・コストゼロ）**
  - 変更検知→500msデバウンス→即保存
  - 保存インジケーター（「保存中...」→「保存済み ✓」）はlocalStorage保存完了に連動
- **2層：Firestore（イベント駆動・ログインユーザーのみ）**
  - 保存タイミング：ページ離脱 / タブ非表示 / プラン切替 / ログアウト時のみ
  - 1セッションあたり2〜5回、DAU 3,000人でも無料枠の45%程度
  - ログアウト時は全プラン強制同期（forceSyncAll）

### 共有の仕組み
- POST /api/share → Firestore → nanoid 8文字短縮URL → /share/:shareId

### デザイン方針
- まず全体を白黒ベースに統一 → あとでアクセントカラーを入れる
- ヘッダー: コンテンツ名は省略なし、プラン名を省略

### モバイル設計方針（確定 2026-03-24）
- 全パネルはMobileBottomSheet方式、ボトムナビ常に最前面
- コントロールバーはモバイル非表示（ツールシートに集約）
- 軽減追加は5列フラットグリッド、連続配置可
- ダメージ表示はモバイルで千単位省略（211k / 1.5M）
- PC版には一切影響させない

### プラン管理方針
- 1コンテンツあたり最大5件 / 合計最大50件
- 30件超過でアーカイブ警告、50件でハードブロック

### FFLogs API管理方針（2026-03-25確定）
- ログイン限定、クライアント1時間15回制限
- APIキー5組ラウンドロビン運用中

### ユーザーフロー（確定方針 2026-03-24）
- プラン0件: サイドバー開、オーバーレイ案内、コンテンツ選択でチュートリアル発火
- プランあり: サイドバー開閉は前回の状態を記憶
- 共有リンク訪問者: チュートリアルは出ない、自分で作ろうとした時に発火

### チュートリアル（現在の状態 2026-03-26更新）
- 全25ステップ実装完了、通し動作確認済み
- 開始ダイアログにJP/EN言語切り替え配置済み（フッター左）
- ブラウザ言語自動検出は未実装（将来検討）
- 将来: オートプラン体験・プラン名編集・共有体験・動画デモ

## 管理者ログイン手順（初回セットアップ）

### 準備
1. **Vercelに環境変数`ADMIN_SECRET`を追加**
   - Vercelダッシュボード → Settings → Environment Variables
   - 名前: `ADMIN_SECRET`
   - 値: 長いランダム文字列（例: 32文字以上の英数字）
   - 対象: Production, Preview, Development すべてにチェック
   - 追加後、再デプロイが必要（Settings → Deployments → 最新を Redeploy）

2. **自分のFirebase UIDを確認**
   - Firebase Console → Authentication → Users → 自分のメールアドレスの行のUID列をコピー

### 管理者ロール設定
3. **ターミナルから以下のコマンドを実行**（UIDとsecretを自分のものに置き換える）
```bash
curl -X POST https://lopoly.app/api/admin/set-role \
  -H "Content-Type: application/json" \
  -d '{"uid":"ここにFirebase UID","role":"admin","secret":"ここにADMIN_SECRET"}'
```
   - 成功: `{"success":true,"uid":"...","role":"admin"}`
   - 失敗: `{"error":"Unauthorized"}` → secretが間違っている

4. **ブラウザでログインし直す**（Custom Claimsの反映にはトークン更新が必要）
   - ログアウト → 再ログイン
   - または: 1時間待つ（トークンの自動更新）

5. **管理画面にアクセス**
   - https://lopoly.app/admin にアクセス → ダッシュボードが表示されれば成功

### トラブルシューティング
- `/admin`にアクセスしてもトップページにリダイレクトされる → ログアウト→再ログインを試す
- curlで403が返る → `ADMIN_SECRET`の値がVercelの環境変数と一致しているか確認
- curlで500が返る → Vercelのログ（Functions タブ）でエラー内容を確認

## 技術メモ
- Vercel無料プラン: 月10万関数実行 / Firebase無料: 月5万アクティブユーザーまでOK
- Twitter OAuth 2.0 + Vercel API方式
- firebase-admin v13 は モジュラーimport
- .env.local に全シークレット保管済み
- 本番URL: https://lopoly.app/
- Discord: https://discord.gg/V288kfPFMG
- Ko-fi: https://ko-fi.com/lopoly
- **backdrop-filterは直書き禁止** → docs/TECH_NOTES.md参照
- **conic-gradientの回転要素は200vmax正方形にすること** → docs/TECH_NOTES.md参照
